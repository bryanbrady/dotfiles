version: 2
jobs:
  build:
    docker:
      - image: bbrady/circleci-env:0.0.0

    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - $FINGERPRINT
      - run:
          .circleci/deploy.sh
